
var FS = require('fs');
var HTTP = require('http');
var Path = require('path');
var Util = require('util');

var Watch = require('fs-watcher');

var Sync = module.exports = function(options) {
	options = options || {};
	var self = this;
	
	
};
